# Keep secrets out of git
.env
# Ignore backup/variant env files but allow a documented example if ever added
.env.*
!.env.example

# Common local files
.DS_Store
node_modules/

# App-runtime artifacts (if any are created locally)
logs/
uploads/
images/
meili_data*/
data-node/






